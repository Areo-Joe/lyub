# Things you must know

- Use bun as package manager
- Use bun as script runner, such as `bunx`
- Use Tailwind and Shadcn. Always look for Shadcn first when a component is needed. If a need a absent Shadcn component, install with `bunx` and Shadcn's cli
- Before writing code, search for best practice and give plan
